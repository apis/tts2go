# kittentts configuration file

# Path to ONNX model file
model_path = "models/model.onnx"

# Path to voice embeddings
# - For Kitten TTS: "models/voices.npz"
# - For Kokoro TTS: "models/voices" (directory with .bin files)
# Leave empty to auto-detect
voices_path = ""

# Output WAV file path
output = "output.wav"

# Voice to use (leave empty to auto-select first available)
# - Kitten voices: expr-voice-2-f, expr-voice-2-m, expr-voice-3-f, etc.
# - Kokoro voices: af_bella, am_adam, bf_emma, bm_george, etc.
voice = ""

# Speech speed multiplier (1.0 = normal)
speed = 1.0

# Log level: "debug", "info", "warn", "error"
log_level = "info"

# Path to log file (empty for stderr)
log_file = ""
